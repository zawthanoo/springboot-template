apiVersion: v1
kind: Service
metadata:
  name: springboot-template
  labels:
    app: springboot-template  
spec:
  selector:
    app: springboot-template
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
